# Server only. Never prefix secrets with VITE_.
# Adapter expects an OpenAI-compatible POST /chat/completions contract.
# Provider endpoint and model supplied by the project owner.
AGNES_API_KEY=
AGNES_BASE_URL=https://apihub.agnes-ai.com/v1
AGNES_TEXT_MODEL=agnes-2.0-flash
PORT=3001
